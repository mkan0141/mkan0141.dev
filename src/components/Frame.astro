<div class="frame-outer relative">
  <div class="frame">
    <div class="frame-inner">
      <slot />
    </div>
  </div>
</div>

<style lang="scss">
  @keyframes rotate {
    0% {
      opacity: 0;
      transform: rotate(65deg);
    }

    100% {
      opacity: 1;
      transform: rotate(0deg);
    }
  }

  @keyframes bound {
    0% {
      transform: rotate(0deg);
    }
    10% {
      transform: rotate(1.75deg);
    }
    20% {
      transform: rotate(0deg);
    }
    30% {
      transform: rotate(0.875deg);
    }
    40% {
      transform: rotate(0deg);
    }
  }

  .frame-outer {
    &::before {
      content: '';
      display: block;
      position: absolute;
      top: 4px;
      left: 2px;
      width: 100%;
      height: 100%;
      background-color: white;
      opacity: 0;
      transform: rotate(45deg);
      animation: rotate 0.8s cubic-bezier(0.55, 0, 1, 0.45) forwards,
        bound 0.8s ease-out 0.8s forwards;
      transform-origin: 100% 100%;
    }

    .frame {
      opacity: 0;
      transform: rotate(45deg);
      animation: rotate 0.8s 0.02s cubic-bezier(0.55, 0, 1, 0.45) forwards,
        bound 0.8s ease-out 0.82s forwards;
      transform-origin: 100% 100%;

      &::after {
        content: '';
        position: absolute;
        top: 0;
        left: -4px;
        display: block;
        width: calc(100% + 11px);
        border-top: 1px solid black;
      }

      &::before {
        content: '';
        position: absolute;
        display: block;
        height: calc(100% + 10px);
        top: -5px;
        right: 0;
        border-right: 1px solid black;
      }

      .frame-inner {
        &::after {
          content: '';
          position: absolute;
          bottom: 0;
          left: -2px;
          display: block;
          width: calc(100%);
          border-bottom: 1px solid black;
        }

        &::before {
          content: '';
          position: absolute;
          display: block;
          height: calc(100% + 6px);
          top: -3px;
          left: 0;
          border-left: 1px solid black;
        }
      }
    }
  }
</style>
